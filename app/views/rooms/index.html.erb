<h1>Rooms#index</h1>
<p>Find me in app/views/rooms/index.html.erb</p>

<div class="row">
    <div class="col-md-2 bg-dark text-light tabela-user-room"><br>
        <h5>logado como: <%= @current_user.email %></h5><hr>

        <h4>Usuarios on</h4>
        <div>
            <%= turbo_stream_from "users" %>
            <div id="users">
                <%= render @users %>
                <hr>
            </div>
        </div>

        <h4>Salas </h4>
        <%= render partial: "layouts/new_room_form" %><br>
        <div>
            <%= turbo_stream_from "rooms" %>
            <div id="rooms">
                <%= render @rooms %>
            </div>
        </div>
        <br>
    </div>
    <div class="col-md-1"></div>
    <div class="col-md-9 bg-dark chat">
        O chat box esta aki!
    </div>
</div>
